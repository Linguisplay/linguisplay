# 🌐 英文化进度（story.language）

第一刀已上（2026-07-06）：`story.language: "zh"|"en"`（列+迁移+契约+工坊选择器）。
en 剧本 = ①所有 LLM 提示词带【Output language: ENGLISH】指令（runtime.lang_llm 包装器
盖到全部 11 个引擎入口，解析用的中文标记前缀保留）+ ②引擎硬编码旁白走 `_t(content, zh, en)`。
zh 剧本逐字节不变（整个测试套件就是回归网）。

## 已本地化的确定性文案

幕分隔线、时段流转旁白、结局头 4 种+里程碑提示、物证翻查 3 句、收纳/取回、
新去处提示、卡关提示 2 档、关系升级 💗、约定立下/爽约、大限当天、下幕预告。

## 第二刀已做（2026-07-06 晚）

- **玩家端 UI 🌐 切换**：play.html 顶栏 🌐 按钮（localStorage `lp_lang`，reload 生效）；
  EN 字典 + translateNode 树遍历翻静态界面与手机壳/浮层；输入框占位符、手机撰写、
  常用 toast、角色选择等动态点用 t() 包裹；进入 en 剧本自动切换本次会话（未显式选择时）。
- qwen 剩余分支补齐语言指令：opening_hook/farewell/describe_place/start_place/offscreen。
- 首个英文剧本《The Lighthouse at Gull Point》上线（language=en 全链路验证：开场、
  开场钩子、真人 DeepSeek 回合、建议 chips 全英文；门控与 cover 谎言正常）。

## 还欠的（第三刀）

- relationships.name_of 的关系档位名（恋人/暧昧…出现在 💗 文案与 UI chips 里）
- promise_when_label 的 今天/明天/后天；moments 庆祝 toast 的插值文案大部队
- journal/map/charcard 浮层标签；studio.html 整站（作者端暂定中文）
- phone compose 的确定性 fallback 短句、build_suggestions 确定性模板
- **dedash 在英文文本里把「——」换成全角「，」**（应按 story 语言换 ", "）
- **北美主机**：需要买 NA 服务器（阻塞在用户），迁移流程本身就是 tar+systemd 重演

验收路径：Gull Point 全程通关，盯每一条系统文案。
